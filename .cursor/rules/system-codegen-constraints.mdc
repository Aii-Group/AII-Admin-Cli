---
description: 系统级代码生成约束（枚举/Hook/工具函数归位 + React 性能最佳实践 + 业务拆分与命名）
alwaysApply: true
---

# 系统级代码生成约束（必须遵守）

## 1) 枚举统一出口

- **系统级/业务通用的枚举值**：统一放在 `src/enums/index.ts`（集中导出与维护）。
- **禁止**：在业务文件中零散新增 `src/enums/*` 的新枚举文件（除非明确是一次性/强局部且不复用；仍优先评估放回 `index.ts`）。
- **写法统一**（二选一并保持一致，默认推荐 const + type）：
  - 推荐：`export const X = ['a', 'b'] as const` + `export type X = (typeof X)[number]`
  - 或：统一使用 `enum`（不要同仓库混用两套风格）

## 2) React 代码必须符合性能最佳实践

- AI 生成的 React/TSX 代码必须满足 `/vercel-react-best-practices` 的约束。
- 重点倾向（非穷尽）：
  - 避免不必要的重渲染：稳定依赖、避免在 render 中创建不必要的新对象/函数、按需 memoization。
  - 避免瀑布：独立 async 并行、启动 promise 早、await 晚。
  - 优先可读且可维护的性能优化：不要为了“优化”引入复杂度或过度抽象。
- 额外硬约束（用于 AI 自检）：
  - **避免 barrel import**：尽量直接从具体文件路径导入，避免不必要的 bundle 放大。
  - **事件/列定义等引用稳定**：表格 columns、操作函数、回调等需要稳定引用时使用 `useMemo` / `useCallback`。

## 3) 业务文件不要过长：拆组件并按路由 key 归档

- **业务页面文件不应过长**（以可读性/可维护性为目标）：
  - **硬阈值建议**：单文件（`.tsx/.ts`）超过 **250 行**应开始拆分；超过 **400 行**必须拆分（纯数据/文档类常量除外）。
  - 当出现大段 UI 结构、表单块、表格列定义、复杂交互时，必须抽取组件/模块。
- **组件放置位置**：
  - **页面私有组件**：放在 `src/page/<RouteKey>/` 下（建议再细分 `components/`），并按路由 `staticData.key` 进行分类，例如：
  - `src/page/Dashboard/*`
  - `src/page/Table/*`
  - `src/page/Advanced_Table/*`
  - **跨页面复用组件**：统一放在 `src/business-components/` 下（按业务域/能力拆目录），避免散落在 `src/page` 里。
- 目录职责边界：
  - `src/routes/**`：允许承载页面主体（当前工程实际），并包含路由声明（`createFileRoute`）。
    - 当路由文件变大/复杂（参考本节行数阈值）时，必须把可拆分的 UI/模块下沉到 `src/page/<RouteKey>/**`，在 routes 内仅保留“路由声明 + 页面装配（组合 page 下组件）”。
    - **禁止**在 routes 内长期堆叠可复用的表格列/表单项数组/复杂 UI 片段；应拆到 `src/page/**`（页面私有）或 `src/business-components/**`（跨页面复用）。
  - `src/page/**`：承载拆分后的页面模块与页面私有组件（按 `staticData.key` 归档）。
  - `src/business-components/**`：承载跨页面可复用业务组件（不要与页面目录互相引用形成环）。

## 4) 命名必须语义化

- 所有变量/函数/组件命名必须**具体且语义化**，能从名字看出用途与单位/维度：
  - ✅ `userListQuery`, `selectedRowKeys`, `isSubmitting`, `dashboardKpiCards`
  - ❌ `data`, `list`, `tmp`, `a`, `obj`, `handle`（过于泛化）
- 布尔值以 `is/has/can/should` 等前缀表达状态语义。
- 避免歧义缩写：除 `id/URL/HTTP/i18n` 等公认缩写外，不要随意简写。

## 5) Hooks 扩展统一位置

- 新增或扩展 hooks：统一放在 `src/hooks/` 下（与现有 `*.hooks.ts` 体系保持一致），避免散落在页面目录。
- Hook 命名：必须以 `use` 开头，且名称表达业务语义（例如 `useUserTable`、`usePermissionGuard`）。

## 6) 图标优先使用 IconPark

- 业务代码中新增图标时，**优先使用 IconPark**（与项目现有图标体系保持一致）。
- 仅在满足明确条件时例外：
  - 已有 antd 图标被全局大量使用且需要保持一致性
  - 必须使用品牌/定制 SVG（此时优先接入现有 `SvgIcon` 机制）

## 7) 工具函数统一位置（并遵循文件名）

- 工具函数统一收敛在 `src/utils/utils.ts` 中（原 `src/utils/common.ts` 迁移/改名为 `utils.ts`）。
- **禁止**：在业务页面里临时堆“通用工具函数”；应下沉到 `src/utils/utils.ts`，并保持函数粒度小、可复用、命名清晰。
- 边界约束：`src/utils/utils.ts` 只放**真正通用**的小工具；与路由/菜单/HTTP 强相关的工具应放回各自模块（例如 `src/utils/system.ts`、`src/utils/menu.ts`、`src/utils/http/*`），避免 `utils.ts` 变成“杂物间”。

## 8) 接口调用优先使用 TanStack Query（`@tanstack/react-query`）

- **默认优先**：涉及「远程数据」的获取/提交（列表、详情、创建、更新、删除等）优先用 `useQuery` / `useMutation` 管理请求状态与缓存，而不是在组件里直接 `await api()` 或在 `useEffect` 里手写 loading/error。
- **统一收益**：避免重复请求、自动管理 `loading/error`、便于刷新/失效（`invalidateQueries`）、便于并行与重试策略统一。
- **禁止**：在业务组件内到处散落 `axios/fetch` 直调；如确需直调，必须说明其不适合进入 Query Cache 的原因（见下方例外）。
- **推荐**：
  - 查询用 `useQuery`，写操作用 `useMutation`；列表页优先复用项目已有 `useTable`（内部已用 `useMutation` 管理请求与 loading）。
  - `queryKey` 需可复现且稳定（例如 `['user', 'detail', userId]`、`['order', 'list', params]`），避免把临时对象/函数直接塞进 key 导致缓存失效。
  - 请求实现优先复用 `src/utils/http/*` 下的客户端/拦截器体系，不要在页面里新建一套请求器。
- **例外（允许直调）**：
  - **一次性副作用**且不应进入缓存：文件下载/导出、埋点、跳转前的单次校验等。
  - **非 React 上下文**：HTTP 拦截器、store 初始化、工具函数内部（此时仍需保持请求出口集中与可测试）。

## 9) 工具函数优先使用 `lodash-es`

- **默认优先**：常见数组/对象/字符串/函数工具（如 `clamp`、`uniqBy`、`debounce`、`throttle`、`get/set`、`merge`、`isEqual` 等）优先直接使用 `lodash-es`，避免在 `src/utils/utils.ts` 重复造轮子。
- **禁止**：在 `src/utils/utils.ts` 新增与 `lodash-es` 明显同功能的封装（除非语义明确不同，或为了类型/边界行为做了必要约束）。
- **导入要求**：按需从 `lodash-es` 直接导入具体函数（例如 `import { clamp } from 'lodash-es'`），不要整包导入。

